import { View, Text, Image } from 'react-native';
import mainStyles from '../styles/mainStyle';

function Line(props) {
    return (
        
        <View style={{ width: '100%', borderColor: '#9d9d9d', borderWidth: 0.5 }}>
        </View>

    );
}

export default Line;